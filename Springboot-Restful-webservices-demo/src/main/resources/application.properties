spring.datasource.url = jdbc:mysql://localhost:3306/floppy
spring.datasource.username = root
spring.datasource.password = daniel@12345

#spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto = update
server.port = 8080

#Actuator
management.endpoints.web.exposure.include = *
management.info.env.enabled = true
management.endpoint.health.show-details = always

# after below prop it'll show 14 endpoint in actuator and
#http://localhost:8080/actuator/shutdown used to shutdown the app
management.endpoint.shutdown.enabled = true

spring.profiles.active=docker

#
#
#actuator/info
info.app.name = Spring Boot Application
info.app.description = Spring Boot Restful Webservice Demo
info.app.version = 1.0.0 
